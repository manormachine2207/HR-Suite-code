package io.github.manormachine2207.hrsuite.notification;

/** Kinds of in-app notification (ADR-017 Stufe 2). The FE renders each via i18n. */
public enum NotificationType {
    /** An applicant's request reached a terminal decision (APPROVED/REJECTED/COMPLETED). */
    ANTRAG_DECIDED
}
